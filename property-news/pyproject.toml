[project]
name = "varoom-property-news"
version = "0.1.0"
description = "Isolated VaRoom property-news and intelligence subsystem"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
